import React from 'react'

// will display the name of a single activity type
export default function ActivityType({ name }) {

    return (
        <div>
            <p> {name} </p>
        </div>
    )
}
